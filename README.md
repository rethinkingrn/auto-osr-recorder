# auto-osr-recorder
um yea, automatically renders osu replays.
you don't need to already have the map, it will download it automatically
- you need danser already configured lol

## usage
`main.py {path_to_osr}`

## requirements
`requests`
`osrparser`